const menuButton=document.querySelector('.menu-toggle');
const menu=document.querySelector('.main-nav');
menuButton?.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')!=='true';menuButton.setAttribute('aria-expanded',String(open));menu.classList.toggle('open',open);menuButton.textContent=open?'Close':'Menu';});
document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu?.classList.contains('open')){menu.classList.remove('open');menuButton.setAttribute('aria-expanded','false');menuButton.textContent='Menu';menuButton.focus();}});
if(matchMedia('(pointer:fine) and (prefers-reduced-motion:no-preference)').matches){let frame;document.addEventListener('pointermove',e=>{if(frame)cancelAnimationFrame(frame);frame=requestAnimationFrame(()=>{document.body.style.setProperty('--mx',`${e.clientX}px`);document.body.style.setProperty('--my',`${e.clientY}px`);});});document.querySelectorAll('.glow-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width,y=(e.clientY-r.top)/r.height;card.style.setProperty('--rx',`${(0.5-y)*5}deg`);card.style.setProperty('--ry',`${(x-0.5)*5}deg`);card.style.setProperty('--cx',`${x*100}%`);card.style.setProperty('--cy',`${y*100}%`);});card.addEventListener('pointerleave',()=>{card.style.setProperty('--rx','0deg');card.style.setProperty('--ry','0deg');});});}
const search=document.querySelector('[data-search]');const entries=[...document.querySelectorAll('[data-entry]')];let activeFilter='All';
function filterEntries(){if(!search)return;const query=search.value.trim().toLocaleLowerCase();let count=0;entries.forEach(entry=>{const match=entry.textContent.toLocaleLowerCase().includes(query)&&(activeFilter==='All'||entry.dataset.theme===activeFilter);entry.hidden=!match;if(match)count++;});const countNode=document.querySelector('.results-count');if(countNode)countNode.textContent=`${count} ${count===1?'reference':'references'} shown`;const empty=document.querySelector('.empty-message');if(empty)empty.hidden=count!==0;}
search?.addEventListener('input',filterEntries);document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{activeFilter=button.dataset.filter;document.querySelectorAll('[data-filter]').forEach(b=>b.setAttribute('aria-pressed',String(b===button)));filterEntries();}));
